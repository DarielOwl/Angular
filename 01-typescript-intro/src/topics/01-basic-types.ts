const name: string = 'Darius';
let hpPoints: number | 'FULL' = 95;
let attack: number | string = 3000;
let isAlive: boolean = true

hpPoints = 'FULL';

console.log({
    name,hpPoints,isAlive,attack
})


export{}